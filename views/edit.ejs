<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
        <a class="text-blue-500 inline-block mb-10" href="/">Go Back To Home</a>
        <h1 class="text-4xl tracking-tighter">Edit Note</h1>
        <form action="/update/<%= note.id %>" method="post">
            <input class="block px-5 w-full rounded-md outline-none py-2 bg-zinc-800" type="text" name="title" value="<%= note.title %>" required>
            <textarea class="block px-5 w-full rounded-md outline-none mt-2 py-2 bg-zinc-800 resize-none" name="content" required><%= note.content %></textarea>
            <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Update Note">
        </form>
    </div>
</body>
</html>
